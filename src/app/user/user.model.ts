export class User {

    userId: string;
    email: string;
    firstName: string;
    lastName: string;
    
    password: string; // don't actually save any user password info

    public User() {
    }
}